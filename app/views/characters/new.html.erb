<h1>New character</h1>


<%= form_for(@character) do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>